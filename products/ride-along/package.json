{
  "name": "bikebridge-ride-along",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "bun test src/session.test.ts ../shared/heart-rate.test.ts",
    "build": "vite build",
    "build:bundle": "bun run --cwd ../dashboard build && bun run --cwd ../stream-overlay build && bun run build",
    "tauri": "tauri",
    "desktop": "tauri dev",
    "package": "tauri build"
  },
  "dependencies": { "@tauri-apps/api": "^2" },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.1",
    "svelte-check": "^4.7.6",
    "typescript": "~6",
    "vite": "^8.3.0"
  }
}
